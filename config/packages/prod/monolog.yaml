monolog:
    handlers:
        main:
            type: rotating_file
            max_files: 15
            path: "%kernel.logs_dir%/%kernel.environment%.log"
            level: info
            channels: ["!event", "!php"]
            formatter: monolog.formatter.wo_brackets
        console:
            type: console
            process_psr_3_messages: false
            channels: ["!event", "!doctrine"]
